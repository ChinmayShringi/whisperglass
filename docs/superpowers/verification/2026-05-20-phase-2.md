# Phase 2 Verification: Codex Runner

Date: 2026-05-20
Branch: build/phase-2-codex-runner
Task: T2.11 (Phase 2 verification)

Phase 2 adds the Codex runner: the overlay answers questions by spawning the local
`codex exec` CLI, parsing its JSONL stream, and streaming the answer into the UI.
No OpenAI API key is used; authentication relies on the user's local `codex login`
session.

## Automated checks

| Check | Command | Result | Status |
|-------|---------|--------|--------|
| Unit tests | `npm run test` | 23 test files passed, 120 tests passed, 0 failed (duration 1.39s) | PASS |
| Type check | `npm run typecheck` | `typecheck:node` and `typecheck:web` both completed with no errors | PASS |
| Build | `npm run build` | typecheck clean, then electron-vite built main (out/main/index.js, 12.56 kB), preload (out/preload/index.js, 1.43 kB), and renderer (out/renderer/index.html plus CSS and JS assets) with no errors | PASS |

## Real Codex CLI check

The app spawns `codex exec` via `buildCodexArgs` (`src/main/codex/codex-args.ts`).
To confirm the real CLI accepts the app's argument pattern and produces a parseable
stream, the exact command was reproduced directly:

```
mkdir -p /tmp/cc-phase2-verify
codex exec --json --ephemeral --skip-git-repo-check -s read-only \
  -C /tmp/cc-phase2-verify -o /tmp/cc-phase2-verify/out.txt \
  -c 'model_reasoning_effort="low"' "Reply with exactly the word: pong"
```

Environment: `codex` is installed at `/opt/homebrew/bin/codex` (codex-cli 0.125.0)
and the local session is authenticated.

Result (PASS):

- The command exited with code 0.
- Stdout was JSONL: 4 lines, one of which was an `item.completed` event carrying an
  `agent_message` item with `text` equal to `pong`:
  `{"type":"item.completed","item":{"id":"item_0","type":"agent_message","text":"pong"}}`
- The output file `/tmp/cc-phase2-verify/out.txt` was written and contained the
  answer text `pong`.
- Stderr contained two unrelated background warnings (a `codex_models_manager`
  model-refresh timeout and an `rmcp` MCP transport channel close for a local MCP
  endpoint not used by this run). Neither affected the exit code or the parseable
  JSONL stream, so the run is a clean PASS.
- The temp directory `/tmp/cc-phase2-verify` was removed afterward.

This confirms the real `codex exec` CLI accepts the app's flag pattern and emits a
JSONL stream the runner can parse, and writes the answer to the `-o` output file.

## Manual verification checklist

The following checks require launching the GUI app (`npm run dev`) on a real macOS
machine with a display and a human operator. They cannot be exercised by an
automated agent. The project owner should perform each one and tick it off:

- [ ] PENDING USER - The overlay launches with no SetupBanner when Codex is available and authenticated.
- [ ] PENDING USER - Typing a question and pressing Cmd+Return (or clicking Ask) shows the question and a "Thinking..." state.
- [ ] PENDING USER - A real answer streams into the answer panel within roughly 2 to 6 seconds.
- [ ] PENDING USER - The command bar input is disabled while an answer is streaming and re-enabled when it finishes.
- [ ] PENDING USER - Renaming the auth file (`mv ~/.codex/auth.json ~/.codex/auth.json.bak`) and restarting shows the SetupBanner with the "run codex login" message; restore it afterward.
- [ ] PENDING USER - Forcing an error (for example disconnecting the network) shows the error message and a Retry button, and Retry re-runs the last question.

## Summary

The automated portion of Phase 2 passed: 120 unit tests passing across 23 test
files, type checking clean for both the node and web TypeScript projects, a clean
electron-vite production build, and a successful real `codex exec` CLI check
confirming the app's argument pattern produces a parseable JSONL stream and writes
the answer file. The manual GUI checklist above is pending the user's confirmation
on a real macOS machine.

## T2.13 Hardening (3 MEDIUM review findings)

Task: T2.13 (Phase 2 hardening pass)
Branch: build/phase-2-codex-runner

A follow-up hardening pass addressed the 3 MEDIUM-severity findings raised in the
Phase 2 code review. Each finding and its fix:

1. Concurrency cap. Finding: `handleAsk` in `codex-service.ts` could spawn a second
   `codex` subprocess if a new question arrived before the previous one finished.
   Fix: a single-flight `inFlight` guard now rejects a concurrent ask with an
   `AnswerError` instead of starting a second process.

2. Stderr sanitization. Finding: raw subprocess stderr (which can include absolute
   filesystem paths) was surfaced to the renderer, which is visible despite the
   overlay being hidden from screen-shares. Fix: `RunCodexResult` in
   `codex-runner.ts` gained a `diagnostic` field. Raw stderr is routed only into
   `diagnostic` (never emitted to the renderer); the user-facing `error` is
   sanitized.

3. Absolute codex path. Finding: the `codex` binary was located by a bare PATH
   lookup, leaving a PATH-hijack surface. Fix: a new pure module
   `resolve-codex-path.ts` resolves `codex` to an absolute path from a
   known-locations list, falling back to `which` only if those miss. It is wired
   into `index.ts` for both the Codex service and the version check.

Audit verdict: AUDIT: PASS. All 3 findings PASS.

Final automated checks: 141 tests passing across 26 test files, type checking
clean, build clean.
